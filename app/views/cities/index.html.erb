<p id="notice"><%= notice %></p>

  <div class="P_MainTours">

    <h1 class="A_H1">Топ городов</h1>

    <th colspan="3"></th>

      <div class="A_CityList">
        <% @cities.each do |city| %>
          <div class="Please">

           <div class="M_TourCard">

               <%= link_to city do %>

            <% if city.cover.present? %>
              <p>
                <%= image_tag city.cover.card.url%>
              </p>
            <% end %>

            <div class="">
              <h2 class="A_H2"><%= city.name %></h2>
            </div>
            <% end %>
          </div>
        </div>
    <% end %>
  </div>
</div>
