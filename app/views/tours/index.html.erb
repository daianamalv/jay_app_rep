<p id="notice"><%= notice %></p>

<div class="P_MainTours">


<h1 class="A_H1">Топ маршруты по миру</h1>

<th colspan="3"></th>
<div class="O_Tours">



    <% @tours.each do |tour| %>
        <div class="Please">
    <%= link_to tour do%>

        <div class="M_TourCard">
          <div class="">
              <%= image_tag(tour.image, style:'width:500px; height:300px;') if tour.image.attached?%>
          </div>
          <div class="">
            <h2 class="A_H2"><%= tour.title %></h2>
          </div>
          <div class="">
            <h2 class="A_P1"><%= tour.description %></h2>
          </div>
          <div class="">
            <td><%= link_to 'Нравится', tours_add_to_favourite_path(tour_id: tour.id), method: :post, class: "A_P1" %> |</td>
            <td><%= link_to 'Редактировать', edit_tour_path(tour), class: "A_P1" %></td>

          </div>
        </div>
      </div>


        <% end %>
        <% end %>






<br>


</div>
</div>
